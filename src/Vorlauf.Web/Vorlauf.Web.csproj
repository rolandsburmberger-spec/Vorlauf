<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <!--
    Auth läuft als Cookie-Auth mit Demo-Benutzern, Ablage in-memory
    (Domain-Port IProjektStore). Infrastructure liefert PDF (QuestPDF) und
    XRechnung (ZUGFeRD-csharp); beim EF-/Identity-Umstieg kommt dort auch
    der persistente Store her (in Program.cs austauschen).
  -->
  <ItemGroup>
    <ProjectReference Include="..\Vorlauf.Domain\Vorlauf.Domain.csproj" />
    <ProjectReference Include="..\Vorlauf.Infrastructure\Vorlauf.Infrastructure.csproj" />
  </ItemGroup>

</Project>
